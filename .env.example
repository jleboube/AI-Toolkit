# API Keys for AI Services
# Copy this file to .env and fill in your actual API keys

# Gemini API Key (used by multiple services)
GEMINI_API_KEY=your_gemini_api_key_here

# API Key for Hairstyle Changer (if different from GEMINI_API_KEY)
API_KEY=your_api_key_here

# Vite Gemini API Key for URL Shortener
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Domain Configuration
# For local development, use localhost
# For production, use your actual domain (e.g., leboube.ai)
DOMAIN=localhost

# Protocol (http for local, https for production with Cloudflare)
PROTOCOL=http

# Individual Service URLs (automatically constructed from subdomains)
# Leave these as-is, they will be built from DOMAIN variable
# Format: ${PROTOCOL}://subdomain.${DOMAIN} or ${PROTOCOL}://${DOMAIN}:port for localhost

# Subdomain Configuration (used in production)
SUBDOMAIN_HEADSHOT=headshots
SUBDOMAIN_PDF=pdf
SUBDOMAIN_BASEBALL=baseball
SUBDOMAIN_HAIRSTYLE=hairstyle
SUBDOMAIN_MATH=math
SUBDOMAIN_SHORTENER=short

# Note: You may need to create individual .env files in each project directory
# For example: BaseballExitVelo/.env
